const Icon = () => {
  return (
    <div
      style={{
        width: 36,
        height: 36,
        borderRadius: "100%",
        backgroundColor: "blue",
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
      }}
    >
      <span style={{ fontSize: "12px", fontWeight: 500, color: "white" }}>
        AB
      </span>
    </div>
  );
};

export default Icon;
